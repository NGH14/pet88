import { Spinner } from 'components/Spinner/';

export default {
  title: 'COMPONENTS/Spinner',
  component: Spinner,
  parameters: {
    layout: 'centered',
  },
  tags: ['autodocs'],

  argTypes: { onClick: { action: 'closed' } },
};
const 