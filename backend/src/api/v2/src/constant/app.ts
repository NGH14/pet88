export const SERVER_PORT: number = Number(process.env.LOCAL_PORT || 5001);
export const serverListeningMessage: string = `Server listening on ${SERVER_PORT}`;
export const mongoDBConnectMessage: string = `MongoDB Connected`;
